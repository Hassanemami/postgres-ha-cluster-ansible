# Fake three-node inventory used only by the template render test. Every
# host is the control node itself (connection: local) - nothing is
# installed or started, the play only renders templates.
[all]
db1 ansible_host=10.0.0.11
db2 ansible_host=10.0.0.12
db3 ansible_host=10.0.0.13

[all:vars]
ansible_connection=local
