---
collections:
  - name: community.general
    version: ">=8.0.0"
  - name: community.postgresql
    version: ">=3.0.0"
  # ansible.posix provides the firewalld and seboolean modules used on
  # RedHat-family hosts.
  - name: ansible.posix
    version: ">=1.5.0"
